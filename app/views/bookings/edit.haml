%section#rendez-vous
  .container
    .content
      %p#notice= notice
      %h1.title.has-text-primary.has-text-centered.has-lines
        Modifier votre rendez-vous
      = render 'form', booking: @booking
      = link_to 'Voir', @booking
      |
      \#{link_to 'Retour', bookings_path}
